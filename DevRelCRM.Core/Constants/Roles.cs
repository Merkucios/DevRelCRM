namespace DevRelCRM.Core.Constants
{
    // Класс для определения констант ролей пользователей
    public class Roles
    {
        // Константы, представляющие роли пользователей в системе
        public const string Administrator = nameof(Administrator);
        public const string Programmer = nameof(Programmer);
        public const string DevRel = nameof(DevRel);
    }
}
